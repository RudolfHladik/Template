CREATE TABLE Token (
id TEXT NOT NULL PRIMARY KEY,
token TEXT NOT NULL
);

selectAll:
SELECT * FROM Token;

insertToken:
INSERT OR REPLACE INTO Token(id,token)
VALUES (?,?);

deleteAll:
DELETE FROM Token;